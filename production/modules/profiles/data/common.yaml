---
lookup_options:
  profiles::bootstrap::accounts::accounts:
    merge:
      strategy: deep
      merge_hash_arrays: true
  profiles::bootstrap::accounts::groups:
    merge:
      strategy: deep
      merge_hash_arrays: true
  profiles::bootstrap::accounts::sudo_confs:
    merge:
      strategy: deep
      merge_hash_arrays: true
  profiles::bootstrap::repositories::repositories:
    merge:
      strategy: deep
      merge_hash_arrays: true
  profiles::bootstrap::firewall::firewall_rules_common:
    merge:
      strategy: deep
      merge_hash_arrays: true
  profiles::bootstrap::firewall::firewall_default_policies:
    merge:
      strategy: deep
      merge_hash_arrays: true
